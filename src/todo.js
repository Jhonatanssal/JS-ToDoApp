/* class Todo {
  constructor(title, description, date, priority, done) {
    this.title = title;
    this.description = description;
    this.date = date;
    this.priotiry = priority;
    this.done = done;
  }
} */

let one = (e) => {
  alert('work');
  console.log('one');
}
 
export default one;